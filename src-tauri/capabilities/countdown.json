{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "countdown",
  "description": "Capability for the countdown window (receives countdown-tick events)",
  "windows": ["countdown"],
  "permissions": ["core:default"]
}
