{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "recording-hud",
  "description": "Capability for the recording HUD windows (red border + control bar)",
  "windows": ["rec-border", "rec-controls"],
  "permissions": ["core:default"]
}
