{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "recorder",
  "description": "Capability for the recorder mini-editor window (video trim/crop/speed export)",
  "windows": ["recorder"],
  "permissions": [
    "core:default",
    "core:event:default",
    "dialog:allow-save",
    "dialog:allow-message",
    "opener:allow-open-path",
    "opener:allow-reveal-item-in-dir"
  ]
}
